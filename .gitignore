# Python
__pycache__/
*.pyc
.venv/
backend/.venv/
backend/uploads/             # evidence photos (ephemeral on free Render)
# NOTE: backend/static/ is COMMITTED (it holds the vendored Swagger UI
# assets, no CDN dependency). The .gitignore above only excludes
# uploads/ — static/ files will be tracked on git add.
*.db

# Node / React
node_modules/
dashboard/dist/

# Flutter / Dart
.dart_tool/
.flutter-plugins*
build/
mobile/drishti_app/build/
# NOTE: mobile/web_app/ is a COMMITTED flutter-web build used by the Render
# static site (see docs/DEPLOYMENT_PLAN.md Part 4) — tracked on purpose.

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Env & secrets
.env